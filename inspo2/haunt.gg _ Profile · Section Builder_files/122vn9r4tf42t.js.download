(globalThis.TURBOPACK||(globalThis.TURBOPACK=[])).push(["object"==typeof document?document.currentScript:void 0,95065,e=>{"use strict";var t=e.i(972525),r=e.i(462560),n=e.i(721348),u=e.i(946154);let l=[],a=`
#define MAX_COLORS 8
uniform vec2 uCanvas;
uniform float uTime;
uniform float uSpeed;
uniform vec2 uRot;
uniform int uColorCount;
uniform vec3 uColors[MAX_COLORS];
uniform int uTransparent;
uniform float uScale;
uniform float uFrequency;
uniform float uWarpStrength;
uniform vec2 uPointer;
uniform float uMouseInfluence;
uniform float uParallax;
uniform float uNoise;
uniform int uIterations;
uniform float uIntensity;
uniform float uBandWidth;
varying vec2 vUv;

void main() {
  float t = uTime * uSpeed;
  vec2 p = vUv * 2.0 - 1.0;
  p += uPointer * uParallax * 0.1;
  vec2 rp = vec2(p.x * uRot.x - p.y * uRot.y, p.x * uRot.y + p.y * uRot.x);
  vec2 q = vec2(rp.x * (uCanvas.x / uCanvas.y), rp.y);
  q /= max(uScale, 0.0001);
  q /= 0.5 + 0.2 * dot(q, q);
  q += 0.2 * cos(t) - 7.56;
  vec2 toward = (uPointer - rp);
  q += toward * uMouseInfluence * 0.2;

    for (int j = 0; j < 5; j++) {
      if (j >= uIterations - 1) break;
      vec2 rr = sin(1.5 * (q.yx * uFrequency) + 2.0 * cos(q * uFrequency));
      q += (rr - q) * 0.15;
    }

    vec3 col = vec3(0.0);
    float a = 1.0;

    if (uColorCount > 0) {
      vec2 s = q;
      vec3 sumCol = vec3(0.0);
      float cover = 0.0;
      for (int i = 0; i < MAX_COLORS; ++i) {
            if (i >= uColorCount) break;
            s -= 0.01;
            vec2 r = sin(1.5 * (s.yx * uFrequency) + 2.0 * cos(s * uFrequency));
            float m0 = length(r + sin(5.0 * r.y * uFrequency - 3.0 * t + float(i)) / 4.0);
            float kBelow = clamp(uWarpStrength, 0.0, 1.0);
            float kMix = pow(kBelow, 0.3);
            float gain = 1.0 + max(uWarpStrength - 1.0, 0.0);
            vec2 disp = (r - s) * kBelow;
            vec2 warped = s + disp * gain;
            float m1 = length(warped + sin(5.0 * warped.y * uFrequency - 3.0 * t + float(i)) / 4.0);
            float m = mix(m0, m1, kMix);
            float w = 1.0 - exp(-uBandWidth / exp(uBandWidth * m));
            sumCol += uColors[i] * w;
            cover = max(cover, w);
      }
      col = clamp(sumCol, 0.0, 1.0);
      a = uTransparent > 0 ? cover : 1.0;
    } else {
        vec2 s = q;
        for (int k = 0; k < 3; ++k) {
            s -= 0.01;
            vec2 r = sin(1.5 * (s.yx * uFrequency) + 2.0 * cos(s * uFrequency));
            float m0 = length(r + sin(5.0 * r.y * uFrequency - 3.0 * t + float(k)) / 4.0);
            float kBelow = clamp(uWarpStrength, 0.0, 1.0);
            float kMix = pow(kBelow, 0.3);
            float gain = 1.0 + max(uWarpStrength - 1.0, 0.0);
            vec2 disp = (r - s) * kBelow;
            vec2 warped = s + disp * gain;
            float m1 = length(warped + sin(5.0 * warped.y * uFrequency - 3.0 * t + float(k)) / 4.0);
            float m = mix(m0, m1, kMix);
            col[k] = 1.0 - exp(-uBandWidth / exp(uBandWidth * m));
        }
        a = uTransparent > 0 ? max(max(col.r, col.g), col.b) : 1.0;
    }

    col *= uIntensity;

    if (uNoise > 0.0001) {
      float n = fract(sin(dot(gl_FragCoord.xy + vec2(uTime), vec2(12.9898, 78.233))) * 43758.5453123);
      col += (n - 0.5) * uNoise;
      col = clamp(col, 0.0, 1.0);
    }

    vec3 rgb = (uTransparent > 0) ? col * a : col;
    gl_FragColor = vec4(rgb, a);
}
`,o=`
varying vec2 vUv;
void main() {
  vUv = uv;
  gl_Position = vec4(position, 1.0);
}
`,i=e=>{let t=e.replace("#","").trim(),r=3===t.length?`${t[0]??"0"}${t[0]??"0"}${t[1]??"0"}${t[1]??"0"}${t[2]??"0"}${t[2]??"0"}`:t.padEnd(6,"0"),u=parseInt(r.slice(0,2),16)/255,l=parseInt(r.slice(2,4),16)/255,a=parseInt(r.slice(4,6),16)/255;return new n.Vector3(u,l,a)};e.s(["default",0,function({className:e,style:c,rotation:s=90,speed:v=.2,colors:m=l,transparent:f=!0,autoRotate:d=0,scale:p=1,frequency:h=1,warpStrength:y=1,mouseInfluence:g=1,parallax:C=.5,noise:w=.15,iterations:x=1,intensity:q=1.5,bandWidth:R=6}){let S=(0,r.useRef)(null),F=(0,r.useRef)(null),k=(0,r.useRef)(null),I=(0,r.useRef)(null),B=(0,r.useRef)(!0),M=(0,r.useRef)(s),b=(0,r.useRef)(d),P=(0,r.useRef)(null);P.current??=new n.Vector2(0,0);let T=(0,r.useRef)(null);return T.current??=new n.Vector2(0,0),(0,r.useEffect)(()=>{let e=S.current;if(!e)return;let t=new n.Scene,r=new n.OrthographicCamera(-1,1,1,-1,0,1),l=new n.PlaneGeometry(2,2),i=Array.from({length:8},()=>new n.Vector3(0,0,0)),c={uCanvas:{value:new n.Vector2(1,1)},uTime:{value:0},uSpeed:{value:v},uRot:{value:new n.Vector2(1,0)},uColorCount:{value:0},uColors:{value:i},uTransparent:{value:+!!f},uScale:{value:p},uFrequency:{value:h},uWarpStrength:{value:y},uPointer:{value:new n.Vector2(0,0)},uMouseInfluence:{value:g},uParallax:{value:C},uNoise:{value:w},uIterations:{value:x},uIntensity:{value:q},uBandWidth:{value:R}};I.current=c;let s=new n.ShaderMaterial({vertexShader:o,fragmentShader:a,uniforms:c,premultipliedAlpha:!0,transparent:!0}),m=new n.Mesh(l,s);t.add(m);let d=new u.WebGLRenderer({antialias:!1,powerPreference:"high-performance",alpha:!0});F.current=d,d.outputColorSpace=n.SRGBColorSpace,d.setPixelRatio(Math.min(window.devicePixelRatio||1,2)),d.setClearColor(0,+!f),d.domElement.style.width="100%",d.domElement.style.height="100%",d.domElement.style.display="block",e.appendChild(d.domElement);let W=new n.Clock,E=()=>{let t=e.clientWidth||1,r=e.clientHeight||1;d.setSize(t,r,!1),c.uCanvas.value.set(t,r)};E();let A=new ResizeObserver(E);A.observe(e);let O=new IntersectionObserver(e=>{let t=e[0];t&&(B.current=t.isIntersecting,t.isIntersecting&&null===k.current&&(W.start(),k.current=requestAnimationFrame(V)))},{threshold:0});O.observe(e);let L=()=>{document.hidden?null!==k.current&&(cancelAnimationFrame(k.current),k.current=null):B.current&&null===k.current&&(W.start(),k.current=requestAnimationFrame(V))};document.addEventListener("visibilitychange",L);let V=()=>{if(!B.current||document.hidden){k.current=null;return}let e=W.getDelta(),n=W.elapsedTime;c.uTime.value=n;let u=(M.current%360+b.current*n)*Math.PI/180;c.uRot.value.set(Math.cos(u),Math.sin(u));let l=T.current,a=P.current;l&&a&&(l.lerp(a,Math.min(1,8*e)),c.uPointer.value.copy(l)),d.render(t,r),k.current=requestAnimationFrame(V)};return k.current=requestAnimationFrame(V),()=>{null!==k.current&&cancelAnimationFrame(k.current),A.disconnect(),O.disconnect(),document.removeEventListener("visibilitychange",L),l.dispose(),s.dispose(),d.dispose(),d.forceContextLoss(),d.domElement.parentElement===e&&e.removeChild(d.domElement)}},[]),(0,r.useEffect)(()=>{let e=I.current,t=F.current;if(!e)return;M.current=s,b.current=d,e.uSpeed.value=v,e.uScale.value=p,e.uFrequency.value=h,e.uWarpStrength.value=y,e.uMouseInfluence.value=g,e.uParallax.value=C,e.uNoise.value=w,e.uIterations.value=x,e.uIntensity.value=q,e.uBandWidth.value=R;let r=m.filter(Boolean).slice(0,8).map(i);for(let t=0;t<8;t++){let n=e.uColors.value[t],u=r[t];n&&(u?n.copy(u):n.set(0,0,0))}e.uColorCount.value=r.length,e.uTransparent.value=+!!f,t&&t.setClearColor(0,+!f)},[s,d,v,p,h,y,g,C,w,x,q,R,m,f]),(0,r.useEffect)(()=>{let e=S.current;if(!e)return;let t=t=>{let r=e.getBoundingClientRect(),n=(t.clientX-r.left)/(r.width||1)*2-1,u=-((t.clientY-r.top)/(r.height||1)*2-1);P.current?.set(n,u)};return e.addEventListener("pointermove",t),()=>{e.removeEventListener("pointermove",t)}},[]),(0,t.jsx)("div",{ref:S,className:`color-bends-container ${e??""}`,style:c})}])},890486,e=>{e.n(e.i(95065))}]);